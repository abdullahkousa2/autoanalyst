# Copy to .env and paste your Groq key (free at https://console.groq.com).
GROQ_API_KEY=gsk_your_key_here

# Optional overrides:
# AUTOANALYST_MODEL=llama-3.3-70b-versatile
# AUTOANALYST_ALLOW_UPLOAD=1   # 1 = allow CSV upload (local); 0 = samples only (public demo)
