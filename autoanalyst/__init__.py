"""AutoAnalyst — an agentic data analyst.

Give it a DataFrame and a plain-English question; it writes pandas/matplotlib
code, runs it in a persistent sandbox, reads the result, iterates, and answers.
"""

__version__ = "0.1.0"
